<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<view xmlns="http://jmix.io/schema/flowui/view"
      xmlns:c="http://jmix.io/schema/flowui/jpql-condition"
      title="msg://incentiveListView.title"
      focusComponent="incentivesDataGrid">
    <data>
        <collection id="incentivesDc"
                    class="com.company.vzvod.entity.Incentive">
            <fetchPlan extends="_base">
                <property name="userServiceInfo">
                    <property name="user">
                        <property name="firstName"/>
                        <property name="lastName"/>
                        <property name="patronymic"/> <!-- добавили -->
                    </property>
                    <property name="department" fetchPlan="_base"/>
                </property>
            </fetchPlan>
            <loader id="incentivesDl" readOnly="true">
                <query>
                    <![CDATA[select e from Incentive e where :serviceInfo is null or e.userServiceInfo = :serviceInfo]]>
                </query>
            </loader>
        </collection>
    </data>
    <facets>
        <dataLoadCoordinator auto="true"/>
        <urlQueryParameters>
            <pagination component="pagination"/>
        </urlQueryParameters>
    </facets>
    <actions>
        <action id="selectAction" type="lookup_select"/>
        <action id="discardAction" type="lookup_discard"/>
    </actions>
    <layout>
        <dataGrid id="incentivesDataGrid"
                  width="100%"
                  minHeight="20em"
                  dataContainer="incentivesDc"
                  columnReorderingAllowed="true">
            <actions>
                <action id="createAction" type="list_create"/>
                <action id="editAction" type="list_edit"/>
                <action id="removeAction" type="list_remove"/>
            </actions>
            <columns resizable="true">
                <column property="userServiceInfo"/>
                <column property="initiator"/>
                <column property="date"/>
                <column property="orderNumber"/>
                <column property="incentiveType"/>
                <column property="description"/>
            </columns>
        </dataGrid>
        <hbox id="lookupActions" visible="false">
            <button id="selectButton" action="selectAction"/>
            <button id="discardButton" action="discardAction"/>
        </hbox>
        <hbox id="buttonsPanel" classNames="buttons-panel">
            <startSlot>
                <button id="createButton" action="incentivesDataGrid.createAction"/>
                <button id="createMultipleButton"
                        text="msg://incentiveListView.createMultipleButton"/>
                <button id="editButton" action="incentivesDataGrid.editAction"/>
                <button id="removeButton" action="incentivesDataGrid.removeAction"/>
            </startSlot>
            <endSlot>
                <simplePagination id="pagination" dataLoader="incentivesDl"/>
            </endSlot>
        </hbox>
    </layout>
</view>
